# Copy to .env and fill in. .env is gitignored — never commit real keys.

# Pexels API key (required for scheduling) — get one at https://www.pexels.com/api/
PEXELS_API_KEY=

# Optional: explicit path to the zernio CLI if it is not on PATH
# ZERNIO_PATH=/path/to/zernio
